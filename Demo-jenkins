pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Container Info') {
      steps {
        sh 'uname -a'
        sh 'cat /etc/os-release'
        sh 'id'
        sh 'pwd'
      }
    }
  }
}

